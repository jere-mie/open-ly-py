# Bornais-Ly
A simple app simimlar to Bitly, used to replace me needing to 

## About some of the files
* `run-example.sh` is an axample script for running the app in gunicorn
* `secrets-example.json` is an example of what the `secrets.json` file should look like 